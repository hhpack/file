language: php
php:
    - "hhvm"
    - "hhvm-nightly"
before_script:
    - composer self-update
    - composer install --prefer-source
script:
    - composer test
matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm-nightly
